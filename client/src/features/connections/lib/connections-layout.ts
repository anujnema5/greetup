/** List column width — fixed on desktop so it never stretches full row while loading. */
export const CONNECTIONS_LIST_COLUMN_CLASS =
  "w-full max-w-lg shrink-0 grow-0 md:w-xl md:max-w-xl";

export type ConnectionsPageListLayout = "centered" | "split";
