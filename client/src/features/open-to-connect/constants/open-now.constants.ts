/** Home preview: top ranked picks only. */
export const OPEN_NOW_HOME_PREVIEW_LIMIT = 3;

/** Explore preview: top ranked picks only. */
export const OPEN_NOW_EXPLORE_PREVIEW_LIMIT = 4;

/** Full browse page page size (matches server default feed limit). */
export const OPEN_NOW_BROWSE_PAGE_SIZE = 12;

/** Home preview: three cards in one row on desktop. */
export const OPEN_NOW_HOME_GRID_CLASS = "lg:grid-cols-3";

/** Explore preview: four cards in one row on desktop. */
export const OPEN_NOW_EXPLORE_GRID_CLASS = "lg:grid-cols-4";

export const OPEN_NOW_BROWSE_GRID_CLASS = "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3";
