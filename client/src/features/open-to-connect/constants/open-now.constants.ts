/** Home / explore preview: top ranked picks only. */
export const OPEN_NOW_HOME_PREVIEW_LIMIT = 4;

/** Full browse page page size (matches server default feed limit). */
export const OPEN_NOW_BROWSE_PAGE_SIZE = 12;

/** Home preview: four cards in one row (not 2×2). */
export const OPEN_NOW_HOME_GRID_CLASS = "grid grid-cols-4 gap-2 sm:gap-3";

export const OPEN_NOW_BROWSE_GRID_CLASS = "grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3";
