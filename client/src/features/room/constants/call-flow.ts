/** Navigate here after skip or when the direct-match partner drops (see `DirectCallPartnerDisconnectHandler`). */
export const MATCHMAKING_HUB_PATH = "/explore";

/** Avoid reacting to transient empty `remotePeerIds` while SFU state settles. */
export const DIRECT_CALL_PEER_LEFT_DEBOUNCE_MS = 600;
