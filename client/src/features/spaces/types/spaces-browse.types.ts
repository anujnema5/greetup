/** Tab keys for `/spaces` browse UI. */
export type SpacesBrowseTab = "all" | "invited" | "mine" | "discover";

export type SpacesBrowseSectionKey = "invited" | "mine" | "discover";

export type SpacesBrowseSectionCopy = {
  title: string;
  description: string;
  empty: string;
};
