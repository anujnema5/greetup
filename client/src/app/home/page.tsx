import { DashboardPage } from "@/features/dashboard";
import { createPageMetadata } from "@/lib/routing/page-metadata";

export const metadata = createPageMetadata("Home", "Find people who get you.");

export default DashboardPage;
