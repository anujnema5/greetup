import { ProfilePage } from "@/features/profile";
import { createPageMetadata } from "@/lib/routing/page-metadata";

export const metadata = createPageMetadata("Profile", "Your Greetup profile.");

export default ProfilePage;
