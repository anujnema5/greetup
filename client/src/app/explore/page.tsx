import { ExplorePage } from "@/features/explore";
import { createPageMetadata } from "@/lib/routing/page-metadata";

export const metadata = createPageMetadata("Explore", "Discover people and circles.");

export default ExplorePage;
