// TODO: chess game logic
// - createGame(roomId: string, players: string[]): ChessGame
// - applyMove(gameId: string, move: ChessMove): ChessGame
// - resign(gameId: string, peerId: string): ChessGame
// - getGame(gameId: string): ChessGame | null
