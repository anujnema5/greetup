// TODO: ludo game logic
// - createGame(roomId: string, players: string[]): LudoGame
// - rollDice(gameId: string, peerId: string): LudoGame
// - movePiece(gameId: string, move: LudoMove): LudoGame
// - getGame(gameId: string): LudoGame | null
