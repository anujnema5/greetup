// TODO: Redis operations for room data
// - saveRoom(room: RoomRecord): Promise<void>
// - getRoom(roomId: string): Promise<RoomRecord | null>
// - deleteRoom(roomId: string): Promise<void>
// - addPeerToRoom(roomId: string, peerId: string): Promise<void>
// - removePeerFromRoom(roomId: string, peerId: string): Promise<void>
// - getRoomPeerIds(roomId: string): Promise<string[]>
// - getRoomPeerCount(roomId: string): Promise<number>
