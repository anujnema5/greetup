/**
 * Global namespace Socket.IO events for guest call trial.
 * Must stay in sync with client guest-trial socket constants (when added).
 */
export const GUEST_TRIAL_SOCKET_EVENTS = {
  trialConsumed: "guest:trial_consumed",
} as const;
