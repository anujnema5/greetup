/**
 * V1 launch: max people in one circle room (including the host).
 * DB `rooms_max_participants_bounds` may still allow a higher ceiling for future tiers.
 */
export const LAUNCH_MAX_CIRCLE_PARTICIPANTS = 8;
