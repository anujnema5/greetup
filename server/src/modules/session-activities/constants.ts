/** Max detail length stored on junction rows (`detail` / `detail_normalized` columns). */
export const ACTIVITY_DETAIL_STORAGE_MAX = 120;

/** Default per-catalog-row validation cap when seed omits `detailMaxLength`. */
export const ACTIVITY_DETAIL_DEFAULT_MAX = 80;

/** Match prep — quick or activity intent. */
export const MAX_MATCH_PREP_ACTIVITY_SELECTIONS = 3;

/** Group space create / update tagging. */
export const MAX_SPACE_ACTIVITY_SELECTIONS = 5;
